/* @flow */
import React from 'react';
import {
    Text,
    View,
} from 'react-native';

const SearchHistoryScreen = () => (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>SearchHistoryScreen</Text>
    </View>
);

export default SearchHistoryScreen;